#pragma once

#include <string_view>

namespace hitsc {

inline constexpr std::string_view kName = "hitsc";
inline constexpr std::string_view kExpansion = "HTTPS IPMI terminal services client";
inline constexpr std::string_view kVersion = "0.1.0";

} // namespace hitsc
